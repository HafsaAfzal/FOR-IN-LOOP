// Create an array called `numbers` with some numeric values. Use a `for...in` loop to
// iterate over the array and log each index.
const numbers =[24,675,14,7,356];
for (num in numbers){
    console.log(num)
}