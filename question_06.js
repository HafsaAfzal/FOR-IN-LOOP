// Create an object called `car` with properties like `brand`, `model`, and `year`. Use a
// `for...in` loop to iterate over the object and log each property value.
const car ={
    brand:"unique",
    model:"moz-34",
    year:2024
}
for (prop in car ){
    console.log(prop);
}